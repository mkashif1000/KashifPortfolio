I want to Replace the Blog Page on this Project with This

import React, { useState, useEffect, useRef } from 'react';
import { 
  Sun, Moon, ArrowRight, Search, 
  Clock, Tag, ChevronRight, Mail,
  Calendar, Heart, Share2, X,
  Maximize2, Sparkles
} from 'lucide-react';

/* --- THEME & ASSETS --- */
const THEME = {
  dark: {
    bg: 'bg-[#0a0a0f]',
    text: 'text-white',
    muted: 'text-gray-400',
    accent: 'text-purple-400',
    primary: 'bg-purple-600 hover:bg-purple-500',
    border: 'border-white/10',
    glass: 'bg-white/5 backdrop-blur-md border border-white/10',
    glassHover: 'bg-white/10 border-white/20',
    gradient: 'from-purple-900 to-blue-900',
    selection: 'selection:bg-purple-500 selection:text-white',
    modalOverlay: 'bg-black/80 backdrop-blur-sm'
  },
  light: {
    bg: 'bg-[#f8f5f2]',
    text: 'text-slate-900',
    muted: 'text-slate-500',
    accent: 'text-orange-600',
    primary: 'bg-orange-600 hover:bg-orange-500',
    border: 'border-black/5',
    glass: 'bg-white/60 backdrop-blur-md border border-black/5',
    glassHover: 'bg-white/80 border-black/10',
    gradient: 'from-orange-200 to-pink-200',
    selection: 'selection:bg-orange-500 selection:text-white',
    modalOverlay: 'bg-white/50 backdrop-blur-sm'
  }
};

/* --- DUMMY DATA --- */
const CATEGORIES = ['All', 'Development', 'Design', 'Career', 'Tutorials'];

const POSTS = [
  {
    id: 1,
    title: "The Architecture of a Modern MERN Stack",
    excerpt: "Why I stopped using Redux for everything and how React Query changed my database relationships forever. State management doesn't have to be a headache.",
    content: "Full content would go here. We discuss the shift from monolithic state stores to server-state management using tools like TanStack Query. It simplifies the frontend code significantly and improves user experience with out-of-the-box caching and optimistic updates.",
    category: "Development",
    date: "Oct 24, 2025",
    readTime: "8 min read",
    likes: 124,
    featured: true
  },
  {
    id: 2,
    title: "Mastering CSS Grid in 2026",
    excerpt: "Flexbox is great, but Grid is the future of layout. Here is a deep dive into subgrid and alignment.",
    content: "CSS Grid Level 2 includes Subgrid, which allows nested grids to participate in the sizing of their parent grid. This article explores practical examples of magazine layouts built entirely with CSS Grid.",
    category: "Design",
    date: "Oct 12, 2025",
    readTime: "5 min read",
    likes: 89
  },
  {
    id: 3,
    title: "From Junior to Senior: A Roadmap",
    excerpt: "The soft skills and hard truths I learned scaling the corporate ladder in the tech industry.",
    content: "It's not just about code. Senior engineers act as multipliers for their team. We talk about mentorship, system design thinking, and how to communicate technical debt to stakeholders.",
    category: "Career",
    date: "Sep 28, 2025",
    readTime: "6 min read",
    likes: 256
  },
  {
    id: 4,
    title: "Building Accessible Forms",
    excerpt: "Accessibility isn't optional. Learn how to create inclusive forms that everyone can use.",
    content: "Semantic HTML is the first step. We cover aria-labels, focus management, and keyboard navigation to ensure your React forms are usable by everyone, including screen reader users.",
    category: "Tutorials",
    date: "Sep 15, 2025",
    readTime: "4 min read",
    likes: 45
  },
  {
    id: 5,
    title: "Why I Love Tailwind CSS",
    excerpt: "Utility-first CSS might look ugly in HTML, but it's beautiful for productivity.",
    content: "The speed of development with Tailwind is unmatched. We look at how the JIT compiler works and how to set up a design system using Tailwind config.",
    category: "Development",
    date: "Aug 30, 2025",
    readTime: "3 min read",
    likes: 312
  }
];

/* --- COMPONENTS --- */

// 1. Scroll Progress Bar
const ScrollProgress = ({ isDark }) => {
  const [width, setWidth] = useState(0);

  useEffect(() => {
    const handleScroll = () => {
      const totalHeight = document.documentElement.scrollHeight - window.innerHeight;
      const progress = (window.scrollY / totalHeight) * 100;
      setWidth(progress);
    };
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  return (
    <div className="fixed top-0 left-0 w-full h-1 z-[60]">
      <div 
        className={`h-full transition-all duration-100 ease-out ${isDark ? 'bg-gradient-to-r from-purple-500 to-blue-500' : 'bg-gradient-to-r from-orange-500 to-pink-500'}`}
        style={{ width: `${width}%` }}
      />
    </div>
  );
};

// 2. Interactive Tilt Card
const TiltCard = ({ children, className, onClick, isDark }) => {
  const cardRef = useRef(null);
  const [rotation, setRotation] = useState({ x: 0, y: 0 });
  const [spotlight, setSpotlight] = useState({ x: 50, y: 50, opacity: 0 });

  const handleMouseMove = (e) => {
    if (!cardRef.current) return;
    
    const rect = cardRef.current.getBoundingClientRect();
    const x = e.clientX - rect.left;
    const y = e.clientY - rect.top;
    
    // Calculate rotation
    const centerX = rect.width / 2;
    const centerY = rect.height / 2;
    const rotateX = ((y - centerY) / centerY) * -3; // Max 3deg tilt
    const rotateY = ((x - centerX) / centerX) * 3;
    
    setRotation({ x: rotateX, y: rotateY });
    setSpotlight({ x, y, opacity: 1 });
  };

  const handleMouseLeave = () => {
    setRotation({ x: 0, y: 0 });
    setSpotlight({ ...spotlight, opacity: 0 });
  };

  return (
    <div 
      ref={cardRef}
      onClick={onClick}
      onMouseMove={handleMouseMove}
      onMouseLeave={handleMouseLeave}
      className={`relative transform-gpu transition-transform duration-200 ease-out preserve-3d cursor-pointer ${className}`}
      style={{
        transform: `perspective(1000px) rotateX(${rotation.x}deg) rotateY(${rotation.y}deg) scale3d(1, 1, 1)`
      }}
    >
      {/* Spotlight Effect */}
      <div 
        className="pointer-events-none absolute -inset-px opacity-0 transition-opacity duration-300 z-10 rounded-2xl"
        style={{
          opacity: spotlight.opacity,
          background: `radial-gradient(600px circle at ${spotlight.x}px ${spotlight.y}px, ${isDark ? 'rgba(255,255,255,0.1)' : 'rgba(0,0,0,0.05)'}, transparent 40%)`
        }}
      />
      {children}
    </div>
  );
};

// 3. Quick View Modal
const PostModal = ({ post, isOpen, onClose, isDark, t }) => {
  if (!isOpen || !post) return null;

  return (
    <div className={`fixed inset-0 z-[100] flex items-center justify-center p-4 ${t.modalOverlay}`} onClick={onClose}>
      <div 
        className={`relative w-full max-w-2xl max-h-[80vh] overflow-y-auto rounded-3xl p-8 shadow-2xl transform transition-all animate-in fade-in zoom-in-95 duration-300 ${t.bg} border ${t.border}`}
        onClick={e => e.stopPropagation()}
      >
        <button 
          onClick={onClose}
          className={`absolute top-4 right-4 p-2 rounded-full transition-colors ${isDark ? 'hover:bg-white/10' : 'hover:bg-black/5'}`}
        >
          <X size={24} />
        </button>

        <div className="flex items-center gap-2 text-xs font-bold uppercase tracking-wider opacity-60 mb-4">
          <span className={t.accent}>{post.category}</span>
          <span>•</span>
          <span>{post.readTime}</span>
        </div>

        <h2 className="font-display text-3xl md:text-4xl font-bold mb-6 leading-tight">
          {post.title}
        </h2>

        <div className={`w-full h-64 rounded-xl mb-8 ${isDark ? 'bg-gray-800' : 'bg-gray-200'} overflow-hidden relative`}>
            <div className={`absolute inset-0 bg-gradient-to-br ${t.gradient} opacity-50`}></div>
            <div className="absolute inset-0 flex items-center justify-center opacity-20">
              <Sparkles size={64} />
            </div>
        </div>

        <div className={`prose prose-lg ${isDark ? 'prose-invert' : ''} mb-8 font-body leading-loose opacity-90`}>
          <p>{post.excerpt}</p>
          <p>{post.content}</p>
        </div>

        <div className={`flex items-center justify-between pt-6 border-t ${t.border}`}>
            <div className="flex gap-4">
                <button className="flex items-center gap-2 text-sm font-bold opacity-70 hover:opacity-100 hover:text-pink-500 transition-all">
                    <Heart size={18} /> {post.likes}
                </button>
                <button className="flex items-center gap-2 text-sm font-bold opacity-70 hover:opacity-100 transition-all">
                    <Share2 size={18} /> Share
                </button>
            </div>
            <button className={`px-6 py-2 rounded-full text-sm font-bold ${t.primary} text-white shadow-lg`}>
                Read Full Article
            </button>
        </div>
      </div>
    </div>
  );
};

// 4. Alive Background (Preserved)
const AliveBackground = ({ isDark }) => {
  const canvasRef = useRef(null);

  useEffect(() => {
    const canvas = canvasRef.current;
    const ctx = canvas.getContext('2d');
    let animationFrameId;
    let particles = [];
    
    const resize = () => {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    };
    window.addEventListener('resize', resize);
    resize();

    class Particle {
      constructor() {
        this.x = Math.random() * canvas.width;
        this.y = Math.random() * canvas.height;
        this.vx = (Math.random() - 0.5) * 0.3;
        this.vy = (Math.random() - 0.5) * 0.3;
        this.size = Math.random() * 2 + 1;
        this.color = isDark 
          ? `hsla(${Math.random() * 60 + 240}, 100%, 70%, 0.2)` 
          : `hsla(${Math.random() * 40 + 10}, 100%, 50%, 0.15)`;
      }
      update() {
        this.x += this.vx;
        this.y += this.vy;
        if (this.x < 0 || this.x > canvas.width) this.vx *= -1;
        if (this.y < 0 || this.y > canvas.height) this.vy *= -1;
      }
      draw() {
        ctx.beginPath();
        ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
        ctx.fillStyle = this.color;
        ctx.fill();
      }
    }

    for (let i = 0; i < 35; i++) particles.push(new Particle());

    const animate = () => {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      particles.forEach(p => {
        p.update();
        p.draw();
      });
      animationFrameId = requestAnimationFrame(animate);
    };
    animate();
    return () => {
      window.removeEventListener('resize', resize);
      cancelAnimationFrame(animationFrameId);
    };
  }, [isDark]);

  return <canvas ref={canvasRef} className="fixed top-0 left-0 w-full h-full -z-10 transition-opacity duration-1000" style={{ opacity: 0.6 }} />;
};

// 5. Main App
export default function App() {
  const [isDark, setIsDark] = useState(true);
  const [activeCategory, setActiveCategory] = useState('All');
  const [searchQuery, setSearchQuery] = useState('');
  const [selectedPost, setSelectedPost] = useState(null);
  const [likedPosts, setLikedPosts] = useState({});

  const t = isDark ? THEME.dark : THEME.light;
  
  // Handle Like Animation
  const toggleLike = (e, postId) => {
    e.stopPropagation(); // Prevent card click
    setLikedPosts(prev => ({
      ...prev,
      [postId]: !prev[postId]
    }));
  };
  
  // Filter Logic
  const filteredPosts = POSTS.filter(post => {
    const matchesCategory = activeCategory === 'All' || post.category === activeCategory;
    const matchesSearch = post.title.toLowerCase().includes(searchQuery.toLowerCase());
    return matchesCategory && matchesSearch;
  });

  const featuredPost = filteredPosts.find(p => p.featured) || filteredPosts[0];
  const regularPosts = filteredPosts.filter(p => p.id !== featuredPost?.id);

  return (
    <div className={`min-h-screen transition-colors duration-500 font-sans ${t.bg} ${t.text} ${t.selection}`}>
      <style>{`
        @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;700&family=Syne:wght@400;700;800&display=swap');
        .font-display { font-family: 'Syne', sans-serif; }
        .font-body { font-family: 'Outfit', sans-serif; }
        .preserve-3d { transform-style: preserve-3d; }
      `}</style>

      <ScrollProgress isDark={isDark} />
      <AliveBackground isDark={isDark} />
      <PostModal post={selectedPost} isOpen={!!selectedPost} onClose={() => setSelectedPost(null)} isDark={isDark} t={t} />

      {/* --- NAVIGATION --- */}
      <nav className={`fixed w-full z-50 py-6 px-6 ${t.glass} border-b-0 border-b ${t.border}`}>
        <div className="container mx-auto flex justify-between items-center">
          <a href="#" className="font-display font-bold text-2xl tracking-tighter hover:scale-105 transition-transform">
            KASHIF<span className={t.accent}>.</span>BLOG
          </a>
          
          <div className="flex items-center gap-6">
            <a href="#" className="hidden md:block text-sm font-bold opacity-60 hover:opacity-100 transition-opacity">PORTFOLIO</a>
            <button 
              onClick={() => setIsDark(!isDark)}
              className="p-2 rounded-full hover:bg-gray-500/10 transition-colors transform active:scale-90 duration-200"
            >
              {isDark ? <Sun size={20} /> : <Moon size={20} />}
            </button>
          </div>
        </div>
      </nav>

      {/* --- HEADER --- */}
      <header className="pt-40 pb-20 px-6">
        <div className="container mx-auto text-center max-w-4xl animate-in fade-in slide-in-from-bottom-8 duration-700">
          <div className={`inline-flex items-center gap-2 px-3 py-1 rounded-full text-xs font-bold tracking-wider mb-6 border ${t.border} bg-opacity-10`}>
            <span className="w-2 h-2 rounded-full bg-blue-500 animate-pulse"></span>
            WRITING & THOUGHTS
          </div>
          <h1 className="font-display text-5xl md:text-7xl font-bold mb-6">
            Insights on <span className={t.accent}>Code</span>, <br />
            Design & Future.
          </h1>
          <p className="font-body text-lg opacity-60 max-w-xl mx-auto leading-relaxed">
            A collection of tutorials, opinions, and experiments from my journey as a MERN Stack Developer.
          </p>
        </div>
      </header>

      {/* --- FILTERS & SEARCH --- */}
      <section className="px-6 mb-16 sticky top-24 z-40">
        <div className={`container mx-auto p-4 rounded-2xl ${t.glass} flex flex-col md:flex-row justify-between items-center gap-4 transition-all duration-300 shadow-xl`}>
          
          <div className="flex flex-wrap gap-2 justify-center">
            {CATEGORIES.map(cat => (
              <button
                key={cat}
                onClick={() => setActiveCategory(cat)}
                className={`px-4 py-2 rounded-full text-xs font-bold transition-all duration-300 ${
                  activeCategory === cat 
                    ? `${t.primary} text-white shadow-lg scale-105` 
                    : 'hover:bg-gray-500/10 opacity-60 hover:opacity-100'
                }`}
              >
                {cat}
              </button>
            ))}
          </div>

          <div className={`flex items-center px-4 py-2 rounded-full border ${t.border} bg-opacity-50 w-full md:w-auto focus-within:ring-2 focus-within:ring-current transition-all`}>
            <Search size={16} className="opacity-50 mr-2" />
            <input 
              type="text" 
              placeholder="Search articles..." 
              value={searchQuery}
              onChange={(e) => setSearchQuery(e.target.value)}
              className="bg-transparent outline-none text-sm w-full md:w-48 placeholder-opacity-50 placeholder-current"
            />
          </div>
        </div>
      </section>

      {/* --- CONTENT GRID --- */}
      <main className="container mx-auto px-6 pb-24">
        
        {/* Featured Post (Wrapped in TiltCard) */}
        {featuredPost && (
          <TiltCard 
            isDark={isDark} 
            onClick={() => setSelectedPost(featuredPost)}
            className="mb-16 rounded-3xl group"
          >
            <div className={`relative w-full h-[500px] rounded-3xl overflow-hidden mb-8 shadow-2xl ${isDark ? 'bg-gray-800' : 'bg-gray-200'}`}>
               <div className={`absolute inset-0 bg-gradient-to-tr ${t.gradient} opacity-80 group-hover:scale-105 transition-transform duration-700 ease-out`}></div>
               <div className="absolute inset-0 flex items-center justify-center">
                 <span className="font-display text-9xl font-bold opacity-10 mix-blend-overlay">FEATURED</span>
               </div>
               
               <div className="absolute bottom-0 left-0 p-8 md:p-12 w-full md:w-2/3 z-20">
                 <div className="flex items-center gap-3 text-xs font-bold tracking-wider mb-4 text-white/80">
                    <span className="bg-white/20 backdrop-blur px-2 py-1 rounded">{featuredPost.category}</span>
                    <span>{featuredPost.date}</span>
                 </div>
                 <h2 className="font-display text-4xl md:text-5xl font-bold text-white mb-4 leading-tight group-hover:underline decoration-2 underline-offset-4">
                   {featuredPost.title}
                 </h2>
                 <p className="text-white/90 text-lg mb-6 line-clamp-2 max-w-xl font-body">
                   {featuredPost.excerpt}
                 </p>
                 <button className="flex items-center gap-2 text-white font-bold text-sm bg-white/20 backdrop-blur px-5 py-3 rounded-full hover:bg-white/30 transition-colors">
                   Read Story <ArrowRight size={16} />
                 </button>
               </div>
            </div>
          </TiltCard>
        )}

        {/* Regular Grid (Animated Transition) */}
        <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
          {regularPosts.map((post) => (
            <TiltCard 
              key={post.id} 
              isDark={isDark}
              onClick={() => setSelectedPost(post)}
              className={`flex flex-col h-full rounded-2xl p-6 ${t.glass} hover:shadow-2xl hover:-translate-y-2 transition-all duration-300`}
            >
              <div className={`w-full aspect-[16/9] rounded-xl mb-6 overflow-hidden relative ${isDark ? 'bg-gray-800' : 'bg-gray-200'}`}>
                <div className={`absolute inset-0 bg-gradient-to-br ${t.gradient} opacity-60 transition-transform duration-500`}></div>
                <div className="absolute top-4 right-4 bg-black/20 backdrop-blur px-2 py-1 rounded text-[10px] font-bold text-white flex items-center gap-1">
                  <Clock size={10} /> {post.readTime}
                </div>
              </div>

              <div className="flex-1 flex flex-col z-20">
                <div className="flex items-center gap-2 text-xs font-bold opacity-50 mb-3 uppercase tracking-wider">
                  <Tag size={12} />
                  <span className={t.accent}>{post.category}</span>
                </div>

                <h3 className="font-display text-2xl font-bold mb-3 leading-tight group-hover:text-current transition-colors">
                  {post.title}
                </h3>
                
                <p className="text-sm opacity-60 mb-6 leading-relaxed flex-1">
                  {post.excerpt}
                </p>

                <div className="flex items-center justify-between pt-6 border-t border-gray-500/10">
                  <div className="flex items-center gap-4 text-xs font-bold opacity-50">
                     <span className="flex items-center gap-1">
                        <Calendar size={12} /> {post.date}
                     </span>
                  </div>
                  
                  <div className="flex gap-2">
                    <button 
                      onClick={(e) => toggleLike(e, post.id)}
                      className={`p-2 rounded-full transition-colors ${likedPosts[post.id] ? 'text-pink-500 bg-pink-500/10' : 'hover:bg-gray-500/10'}`}
                    >
                      <Heart size={16} fill={likedPosts[post.id] ? "currentColor" : "none"} />
                    </button>
                    <button className={`p-2 rounded-full hover:bg-gray-500/10 transition-colors`}>
                        <Maximize2 size={16} />
                    </button>
                  </div>
                </div>
              </div>
            </TiltCard>
          ))}
        </div>

        {/* Empty State */}
        {filteredPosts.length === 0 && (
          <div className="text-center py-20 opacity-50 animate-in fade-in zoom-in duration-500">
            <Search size={48} className="mx-auto mb-4 opacity-50" />
            <h3 className="text-xl font-bold">No articles found</h3>
            <p>Try adjusting your search or category filter.</p>
          </div>
        )}

      </main>

      {/* --- NEWSLETTER --- */}
      <section className="container mx-auto px-6 mb-24">
        <div className={`relative overflow-hidden rounded-3xl p-12 md:p-20 text-center transition-all hover:scale-[1.01] duration-500 ${isDark ? 'bg-purple-900/20 border border-purple-500/20' : 'bg-orange-100 border border-orange-200'}`}>
          <div className="relative z-10 max-w-xl mx-auto">
            <div className="inline-block p-4 rounded-full bg-gradient-to-br from-pink-500 to-purple-600 text-white mb-6 shadow-lg shadow-purple-500/30 animate-bounce">
              <Mail size={32} />
            </div>
            <h2 className="font-display text-3xl md:text-5xl font-bold mb-4">Stay in the loop</h2>
            <p className="opacity-70 mb-8 text-lg">
              Get the latest articles, tutorials, and resources delivered straight to your inbox. No spam, ever.
            </p>
            
            <form className="flex flex-col sm:flex-row gap-4" onSubmit={e => e.preventDefault()}>
              <input 
                type="email" 
                placeholder="Enter your email address" 
                className={`flex-1 px-6 py-4 rounded-full outline-none focus:ring-2 transition-all ${isDark ? 'bg-black/30 border border-white/10 focus:ring-purple-500' : 'bg-white border border-black/5 focus:ring-orange-500'}`}
              />
              <button className={`px-8 py-4 rounded-full font-bold uppercase tracking-wide shadow-lg hover:transform hover:scale-105 active:scale-95 transition-all ${t.primary} text-white`}>
                Subscribe
              </button>
            </form>
          </div>

          <div className={`absolute top-0 left-0 w-64 h-64 rounded-full blur-3xl opacity-20 -translate-x-1/2 -translate-y-1/2 animate-pulse ${isDark ? 'bg-purple-500' : 'bg-orange-500'}`}></div>
          <div className={`absolute bottom-0 right-0 w-64 h-64 rounded-full blur-3xl opacity-20 translate-x-1/2 translate-y-1/2 animate-pulse ${isDark ? 'bg-blue-500' : 'bg-pink-500'}`}></div>
        </div>
      </section>

      {/* --- FOOTER --- */}
      <footer className="py-12 border-t border-gray-500/10 text-center">
        <div className="font-display font-bold text-xl mb-4 tracking-tighter">
            KASHIF<span className={t.accent}>.</span>
        </div>
        <div className="flex justify-center gap-6 mb-8 text-sm font-bold opacity-60">
          <a href="#" className="hover:opacity-100 hover:text-purple-400 transition-colors">HOME</a>
          <a href="#" className="hover:opacity-100 hover:text-blue-400 transition-colors">TWITTER</a>
          <a href="#" className="hover:opacity-100 hover:text-gray-400 transition-colors">GITHUB</a>
          <a href="#" className="hover:opacity-100 hover:text-blue-600 transition-colors">LINKEDIN</a>
        </div>
        <p className="text-xs opacity-40">&copy; 2026 Kashif Portfolio. All rights reserved.</p>
      </footer>

    </div>
  );
}

This is a Full Code, So Dont Use Header and Footer From Here
Also Create Proper Components